{% extends 'base.html.twig' %}

{% block metatitle %}Confirmer la suppression | Renault 21 TD{% endblock %}



{% block main %}
    <h1>Confirmer la suppression</h1>
    <div class="alert alert-warning">
        <p>Êtes-vous sûr de vouloir supprimer l'annonce <strong>{{ annonce.title }}</strong> ?</p>
    </div>
    <form action="{{ path('guest/annonces/delete', {id: annonce.id}) }}" method="post">
        <button type="submit" class="btn btn-danger">Oui, supprimer</button>
        <a href="{{ path('guest/annonce-show', {id: annonce.id}) }}" class="btn btn-secondary">Annuler</a>
    </form>
    <p>Cette action est irréversible.</p>
{% endblock %}

